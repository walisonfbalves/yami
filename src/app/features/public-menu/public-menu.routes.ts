import { Routes } from '@angular/router';
import { MenuPageComponent } from './menu-page/menu-page.component';

export const publicMenuRoutes: Routes = [
  { path: '', component: MenuPageComponent }
];
